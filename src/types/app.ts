import { Time, CalendarDate } from "@internationalized/date";
export interface IForm {
  input: string;
  date: CalendarDate;
  time: Time;
  select: string;
  date1: CalendarDate;
  date2: CalendarDate;
}

